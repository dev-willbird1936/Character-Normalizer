# Ownership Marker

Project owner watermark: `dev-willbird1936`.

This marker is intentionally repeated in package metadata, desktop title, UI metadata, CSS overlay, documentation images, and PNG metadata.
